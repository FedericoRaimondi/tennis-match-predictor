name: Training Pipeline

# This workflow is PAUSED - uncomment 'on' section to enable
# on:
#   schedule:
#     # Run monthly on the 1st at 00:00 UTC
#     - cron: '0 0 1 * *'
#   workflow_dispatch:
#     inputs:
#       tune_hyperparameters:
#         description: 'Perform hyperparameter tuning'
#         required: false
#         default: 'false'
#         type: boolean

on:
  workflow_dispatch:  # Manual trigger only for now
    inputs:
      tune_hyperparameters:
        description: 'Perform hyperparameter tuning'
        required: false
        default: 'false'
        type: boolean

jobs:
  check-new-data:
    runs-on: ubuntu-latest
    outputs:
      should_train: ${{ steps.check.outputs.should_train }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: |
          uv sync

      - name: Check for new data
        id: check
        run: |
          # Use the Prefect-based monitoring flow to check for new data
          # Note: This is a simplified check; full monitoring runs in the monitoring workflow
          echo "should_train=true" >> $GITHUB_OUTPUT

  train-model:
    runs-on: ubuntu-latest
    needs: check-new-data
    if: needs.check-new-data.outputs.should_train == 'true'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: |
          uv sync

      - name: Run training pipeline with Prefect
        run: |
          if [ "${{ github.event.inputs.tune_hyperparameters }}" = "true" ]; then
            uv run train-model --tune
          else
            uv run train-model
          fi

      - name: Commit and push new model
        run: |
          git config --global user.name 'GitHub Actions'
          git config --global user.email 'actions@github.com'
          git add models/champion_model.pkl || true
          git commit -m "Update champion model [skip ci]" || echo "No changes to commit"
          git push || echo "No changes to push"

      - name: Create release on promotion
        if: success()
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: model-${{ github.run_number }}
          release_name: Model Training Run ${{ github.run_number }}
          body: |
            Automated model training completed successfully.
            
            Training metrics are available in MLflow.
          draft: false
          prerelease: false
